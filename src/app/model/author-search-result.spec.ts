import {AuthorSearchResult} from './author-search-result';

describe('AuthorSearchResult', () => {
  it('should create an instance', () => {
    expect(new AuthorSearchResult()).toBeTruthy();
  });
});
